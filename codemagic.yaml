workflows:
  ios-workflow:
    name: iOS Build OBPC
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      node: latest
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: |
          npm install
          npm install @capacitor/ios @capacitor/core @capacitor/cli
      - name: Capacitor Sync
        script: |
          # Isso atualiza o Bundle ID dentro da pasta ios para br.com.obpcschool
          npx cap sync ios
      - name: Build iOS (.ipa)
        script: |
          cd ios/App
          agvtool new-version -all $(date +%Y%m%d%H%M)
          
          # Tenta usar os perfis (Certifique-se de que o perfil no site é br.com.obpcschool)
          xcode-project use-profiles --warn-only
          
          # Faz o build usando o ID atualizado
          xcode-project build-ipa \
            --project "App.xcodeproj" \
            --scheme "App" \
            --clean
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - ferna@example.com
